#include "city.h"
#include <iostream>
using namespace std;

city::city(string name, double latitude, double longitude){}

string city::getCity(string c) const
{
	c = name;
	return string(c);
}
